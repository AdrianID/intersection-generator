/**
 * Stub ShaderHandler - direferensikan di index.html tapi tidak dipakai (pemakaian dikomentari).
 * Ada di sini agar script load tidak gagal (ERR_FILE_NOT_FOUND).
 */
function ShaderHandler() {
    // Kosong - tidak dipakai di Road Generator
}
